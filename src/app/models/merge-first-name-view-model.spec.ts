import { MergeFirstNameViewModel } from './merge-first-name-view-model';

describe('MergeFirstNameViewModel', () => {
  it('should create an instance', () => {
    expect(new MergeFirstNameViewModel()).toBeTruthy();
  });
});
