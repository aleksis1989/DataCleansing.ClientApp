import { CleansingFirstNameViewModel } from './cleansing-first-name-view-model';

describe('CleansingFirstNameViewModel', () => {
  it('should create an instance', () => {
    expect(new CleansingFirstNameViewModel()).toBeTruthy();
  });
});
