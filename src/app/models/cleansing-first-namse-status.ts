export enum CleansingFirstNamseStatus {
    // Не процесирани
    // Не процесирани
  NonProcessed = 0,

  // Прифатено Similarity
  AcceptSimilarity = 1,

  // Рачна корекција
  ManualCorrection = 2,

  // Одбиено прочистување
  Rejected = 3,
}
